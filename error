private boolean isOverlayAdded = false;

public void handleFaceDetection(String detectedFaceType) {
    if (detectedFaceType.equals("unknown")) {
        // If overlay is not already added, add it
        if (!isOverlayAdded) {
            addOverlay();
            isOverlayAdded = true; // Set the flag to true
        }
    } else if (detectedFaceType.equals("registered")) {
        // Remove the overlay if a registered user is detected
        if (isOverlayAdded) {
            removeOverlay(); // Implement this method to remove the overlay
            isOverlayAdded = false; // Reset the flag
        }
    }
}

// Method to add overlay (already implemented in your app)
private void addOverlay() {
    // Your existing overlay implementation
}

// Method to remove overlay
private void removeOverlay() {
    // Logic to remove the overlay (e.g., remove the View or reset the transparency)
}